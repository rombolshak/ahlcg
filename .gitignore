# Personal Claude Code overrides — the shared config in .claude/ is checked in.
.claude/settings.local.json

# A Windows program redirecting to the NUL device under Git Bash writes a literal file instead.
# Podman does this from the end-to-end suite, dropping one next to whatever it was run from.
NUL
