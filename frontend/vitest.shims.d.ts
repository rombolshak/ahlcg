/// <reference types="@vitest/browser-playwright" />
// ESLint's typed rules resolve spec files through the nearest tsconfig.json (the root one),
// not tsconfig.spec.json, so the vitest globals have to be declared here to be visible to them.
/// <reference types="vitest/globals" />
