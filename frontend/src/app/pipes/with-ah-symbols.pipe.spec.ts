import { WithAhSymbolsPipe } from './with-ah-symbols.pipe';

describe('WithAhSymbolsPipe', () => {
  it('create an instance', () => {
    const pipe = new WithAhSymbolsPipe();
    expect(pipe).toBeTruthy();
  });
});
