import { CardOutlineDirective } from './card-outline.directive';

describe('CardOutlineDirective', () => {
  it('should create an instance', () => {
    const directive = new CardOutlineDirective();
    expect(directive).toBeTruthy();
  });
});
